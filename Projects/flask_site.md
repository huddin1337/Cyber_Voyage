```bash
sudo apt update && sudo apt upgrade -y

sudo apt install python-pip

sudo apt install python3-venv -y

python3 -m venv venv

source flasksite/bin/activate

pip3 install flask

nano app.py



##to keep it running after leaving ssh:

sudo apt install screen -y

screen

python3 app.py


```

